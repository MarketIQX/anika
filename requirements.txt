# Anika — Python dependencies (Python 3.11)

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
aiofiles>=23

# Config + validation
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# HTTP client
httpx>=0.27

# OpenAI + Agents SDK
openai>=1.55
openai-agents>=0.0.6

# Gmail API (already installed in pilot env)
google-api-python-client>=2.194
google-auth-oauthlib>=1.3
google-auth-httplib2>=0.3

# Email parsing
beautifulsoup4>=4.12
lxml>=5.1

# Vector search — sqlite-vec instead of sqlite-vss.
# Why: sqlite-vss has no Windows wheels as of 2026; sqlite-vec is the
# actively-maintained successor with pre-built Windows support and the same
# cosine/L2 primitives we need for semantic retrieval.
sqlite-vec>=0.1.9

# Misc
python-dateutil>=2.8

# Auth — bcrypt for password hashing, itsdangerous for signed session cookies
# (Starlette's SessionMiddleware uses itsdangerous under the hood).
bcrypt>=4.1
itsdangerous>=2.1

# Teaching-system file extractors + library export.
# Note: pytesseract + pdf2image are pure-Python wrappers that REQUIRE the
# Tesseract OCR binary and Poppler on the host. On Windows install via:
#   choco install tesseract poppler   (or download MSI/zips)
# Without those binaries the extractor flags scanned PDFs as `needs_ocr`
# instead of raising.
pypdf>=5.0
pdfplumber>=0.11
python-docx>=1.1
pytesseract>=0.3
pdf2image>=1.17
openpyxl>=3.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
